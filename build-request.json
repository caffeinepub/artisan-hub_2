{
  "kind": "build_request",
  "title": "Auto-select shape-specific description template in BulkProductUpload",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-139",
      "text": "Update the BulkProductUpload component (frontend/src/components/BulkProductUpload.tsx) to automatically select the matching shape-specific description template for each product based on its shape field value. When a product's shape is set to 'Turtle', 'Dolphin', 'Frog', or 'Whale', automatically apply the corresponding named template (Turtle, Dolphin, Frog, or Whale) to that product's description field. When the shape is set to any other value or is blank, fall back to the 'Default' template. This auto-selection should trigger whenever the shape field changes for a product, and should update the description field with the matched template's content unless the user has manually edited that description field.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1",
          "msg-2"
        ],
        "quotes": [
          "use each template for the descriptions of the same title shape",
          "yes, it fall back to the Default template"
        ]
      },
      "acceptanceCriteria": [
        "When a product's shape field is set to 'Dolphin', the description field is automatically populated with the Dolphin template content.",
        "When a product's shape field is set to 'Turtle', the description field is automatically populated with the Turtle template content.",
        "When a product's shape field is set to 'Frog', the description field is automatically populated with the Frog template content.",
        "When a product's shape field is set to 'Whale', the description field is automatically populated with the Whale template content.",
        "When a product's shape field is set to any value other than Turtle, Dolphin, Frog, or Whale (including blank/empty), the description field falls back to the Default template content.",
        "The auto-selection triggers each time the shape field value changes for a product.",
        "If the user has manually edited the description field for a product, changing the shape does not overwrite their manual edits.",
        "The template dropdown selector continues to function independently, allowing the user to manually override the auto-selected template."
      ]
    }
  ],
  "constraints": [],
  "nonGoals": [
    "Modifying the backend template storage or template content.",
    "Changing the template dropdown UI structure.",
    "Applying shape-based template auto-selection outside of the BulkProductUpload component."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}